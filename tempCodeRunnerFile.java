 (index1 + d + 1 <= n - 1) {
            // swaps = Math.min(swaps, index1 + d + 1);
            // }
            // if (d - index2 + 1 >= 0) {
            // swaps = Math.min(ans, d - index2 + 1);
            // }